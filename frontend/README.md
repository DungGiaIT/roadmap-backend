# VietRecipe Frontend

React + Vite frontend cho VietRecipe.

## Chạy local

```bash
npm install
npm run dev
```

Frontend mặc định chạy tại `http://localhost:5173`.

Backend URL được cấu hình qua `VITE_API_URL`.
